{
  "name": "PassBemAPI",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "expo-server-sdk": "^3.6.0",
    "express": "^4.17.1",
    "express-validator": "^6.14.0",
    "knex": "^0.95.6",
    "moment": "^2.29.1",
    "mongoose": "^5.12.12",
    "multer": "^1.4.2",
    "node-cron": "^3.0.0",
    "nodemon": "^2.0.7",
    "python-shell": "^3.0.0",
    "socket.io": "^4.1.2"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "create-admin": "node src/scripts/createSuperAdmin.js",
    "migrate:filenames": "node scripts/migrate-filenames.js",
    "build": "echo 'Build step not required for this project' && exit 0",
    "health": "curl -f http://localhost:3333/health || exit 1",
    "docker:build": "docker build -f Dockerfile.coolify -t passbem-api .",
    "docker:run": "docker run -p 3333:3333 passbem-api"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
